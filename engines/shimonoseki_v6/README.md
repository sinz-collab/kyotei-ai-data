# 下関AI shimonoseki_engine_v6.0 candidate

本番v5を直接上書きせず、v6として横持ちする候補実装。

## 固定した主要変更
1. 進入確定後、選手×実コースDB / 実コースST / タイプDBを actual course へ再マッピング。
2. motor_recent の up/down と top2/top3 を日程係数つきで強化。
3. 節間着順+STを1/2/3着別に補正。
4. 展示T順位+SUMを同一群として上限管理。
5. スリットは actual course 順で12分類。進入変更時は強度0.70。
6. 逃げ崩れは「逃げ弱点×相手攻め手×スリット×SUM」の複合一致のみ残差移送。
7. motor×節間×slit×SUM×実コース攻めの複合成立は残差補正（3/4/5条件で段階化）。
8. 結果・オッズは計算入力禁止。

`validate_r5.py` は2026-08-20 下関5Rの保存済み直前条件で候補精度を再現確認するための検証。
